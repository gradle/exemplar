commands = [
    {
        executable = "mkdir"
        args = "demo"
    },
   {
        executable = "cd"
        args = "demo"
   },
   {
        executable = "gradle"
        args = "init"
   },
   {
        executable = "gradle"
        args = "projects --no-configuration-cache"
        expected-output-file = "sample.out"
   }
]
